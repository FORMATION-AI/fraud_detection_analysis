name: Fraud Detection CI

on:
  push:
    branches:
      - feature/streamlit-cleanup
  pull_request:
    branches:
      - master

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Placeholder lint step
        run: echo "TODO: add linting (ruff/flake8)."

      - name: Placeholder tests step
        run: echo "TODO: add test execution (pytest)."
